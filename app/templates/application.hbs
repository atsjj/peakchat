{{#if something}}
  <div class="flex flex-first col-xs-12 m0 p0 border border-red">
    <div class="col-xs-8 text-center border border-blue">
      <div class="visible-xs-block">xs</div>
      <div class="visible-sm-block">sm</div>
      <div class="visible-md-block">md</div>
      <div class="visible-lg-block">lg</div>
    </div>
    <div class="flex-first text-left col-xs-2 border border-blue">{{link-to 'home' 'index'}}</div>
    <div class="text-right col-xs-2 border border-blue">{{link-to 'call' 'call'}}</div>
  </div>
{{/if}}

{{liquid-outlet}}
